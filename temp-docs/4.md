## Error Type
Console TypeError

## Error Message
Failed to fetch


    at SettingsPage.useCallback[fetchOllamaModels] (app/settings/page.tsx:40:30)
    at SettingsPage.useEffect (app/settings/page.tsx:69:7)

## Code Frame
  38 |     setFetchError(null);
  39 |     try {
> 40 |       const response = await fetch(`${url}/models`);
     |                              ^
  41 |       if (!response.ok) throw new Error('Failed to fetch models');
  42 |       const data = await response.json();
  43 |       const modelNames = data.data.map((m: any) => m.id);

Next.js version: 16.1.1 (Turbopack)
